<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>{{ page_title }}</title>

    {{ content_for_header }}

    {{ 'ski-demo.css' | asset_url | stylesheet_tag }}
  </head>

  <body>
    {{ content_for_layout }}

    <script src="{{ 'ski-demo.js' | asset_url }}" defer></script>
  </body>
</html>

